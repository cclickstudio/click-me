name: CD

# TODO: 현재 미완성 워크플로우입니다.
# 아래 Secrets 등록 후 주석 해제하여 활성화하세요.
#
# 필요한 GitHub Secrets:
#   - DOCKERHUB_USERNAME   : Docker Hub 계정명
#   - DOCKERHUB_TOKEN      : Docker Hub Access Token
#   - EC2_HOST             : EC2 퍼블릭 IP 또는 도메인
#   - EC2_USER             : EC2 접속 유저 (ubuntu / ec2-user)
#   - EC2_SSH_KEY          : EC2 PEM 키 전체 내용
#   - NEXT_PUBLIC_API_URL  : 운영 API 주소 (예: https://api.yourdomain.com)
#   - SLACK_WEBHOOK_URL    : (선택) 슬랙 알림

on:
  workflow_dispatch: # 준비되면 push 트리거로 교체
  # push:
  #   branches: [master, main]

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    environment: production

    steps:
      - uses: actions/checkout@v4

      # ── TODO: Docker Hub 로그인 ───────────────────────
      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}

      # ── TODO: Docker Buildx 설정 ──────────────────────
      # - name: Set up Docker Buildx
      #   uses: docker/setup-buildx-action@v3

      # ── TODO: 이미지 빌드 & 푸시 ─────────────────────
      # - name: Build & push backend
      #   uses: docker/build-push-action@v6
      #   with:
      #     context: ./backend
      #     push: true
      #     tags: |
      #       ${{ secrets.DOCKERHUB_USERNAME }}/clickme-backend:latest
      #       ${{ secrets.DOCKERHUB_USERNAME }}/clickme-backend:${{ github.sha }}
      #     cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/clickme-backend:buildcache
      #     cache-to: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/clickme-backend:buildcache,mode=max

      # - name: Build & push frontend
      #   uses: docker/build-push-action@v6
      #   with:
      #     context: ./frontend
      #     push: true
      #     tags: |
      #       ${{ secrets.DOCKERHUB_USERNAME }}/clickme-frontend:latest
      #       ${{ secrets.DOCKERHUB_USERNAME }}/clickme-frontend:${{ github.sha }}
      #     cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/clickme-frontend:buildcache
      #     cache-to: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/clickme-frontend:buildcache,mode=max
      #     build-args: |
      #       NEXT_PUBLIC_API_URL=${{ secrets.NEXT_PUBLIC_API_URL }}

      # ── TODO: EC2 SSH 배포 ────────────────────────────
      # - name: Deploy to EC2
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.EC2_HOST }}
      #     username: ${{ secrets.EC2_USER }}
      #     key: ${{ secrets.EC2_SSH_KEY }}
      #     script: |
      #       set -e
      #       cd ~/clickme
      #       docker pull ${{ secrets.DOCKERHUB_USERNAME }}/clickme-backend:latest
      #       docker pull ${{ secrets.DOCKERHUB_USERNAME }}/clickme-frontend:latest
      #       docker compose -f docker-compose.prod.yml up -d --remove-orphans
      #       docker image prune -f

      # ── TODO: 슬랙 알림 (선택) ────────────────────────
      # - name: Notify deploy result
      #   if: always()
      #   uses: slackapi/slack-github-action@v1
      #   continue-on-error: true
      #   with:
      #     payload: |
      #       {
      #         "text": "${{ job.status == 'success' && '✅' || '❌' }} ClickMe 배포 *${{ job.status }}*\nBranch: `${{ github.ref_name }}` | Commit: `${{ github.sha }}`\n<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|워크플로우 보기>"
      #       }
      #   env:
      #     SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      #     SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK

      - name: 배포 준비 중
        run: echo "CD 파이프라인 틀만 구성된 상태입니다. Secrets 등록 후 주석을 해제하세요."
