# 매니지먼트 작업 정리 및 전수 검증 — 2026-07-03 (일일정리)

> 브랜치: `feat/management-3k` · 검증 주체: Claude(자동) — 코드·백엔드·프론트·.env·Neon DB·Meta 실측 직접 접근, 동일 검증 3회 반복 후 종료.

## 상태표 (검증 매트릭스 — 3회 반복 결과)

| 항목 | 방법 | 1회 | 2회 | 3회 | 판정 |
|---|---|---|---|---|---|
| 백엔드 Ruff | `uv run ruff check .` | ✅ | — | — | ✅ 클린 |
| 백엔드 pytest 전체 | `uv run pytest`(831 tests) | ✅ 831 passed | ✅ 831 passed | ✅ 831 passed (`-p no:randomly` 포함) | ✅ |
| 프론트 ESLint | `pnpm lint` | ✅ (경고 1 — ProjectPanel useEffect deps, 기존·에러 아님) | — | — | ✅ |
| 프론트 프로덕션 빌드 | `pnpm build` | ✅ exit 0 | — | — | ✅ |
| Meta 실측 ↔ 백엔드 API | 계정 인사이트(level=campaign) vs `/campaigns` | ✅ 일치 | ✅ 일치 | ✅ 일치(3회 결과 바이트 동일) | ✅ |
| Neon DB 상태 | `management_meta_connections`·`created_campaigns` 직접 조회 | ✅ | — | — | ✅ |
| 신규 엔드포인트 스모크 | budget/report/rebalance/names/scan/connect | ✅ 전부 200 | ✅ | ✅ | ✅ |

**실측 정합 (Meta ↔ 화면/API)** — 잠재 고객 캠페인 노출 874·클릭 61·지출 ₩19,992 / 트래픽 캠페인 1,371·76·₩9,090. 계정 지갑: 한도 ₩29,090 − 누적 ₩29,082 = 잔액 ₩9(메타 표시 그대로). 월 목표 기본 ₩3,000,000.

## 오늘 반영된 작업 (이 세션)

### 1. 회귀 복구 — 캠페인 목록이 다른 계정으로 바뀐 문제

- **원인**: Meta OAuth 재연결 시 토큰에 광고계정이 특정되지 않으면 `/me/adaccounts` **첫 계정을 추측 바인딩** → 옛 테스트 계정(`act_280907101140794`, "ClickMe Traffic Test")으로 연결이 바뀜.
- **조치**: ① DB 연결 행을 운영 계정(`act_882448327559337`)·영구 토큰으로 갱신(복구). ② 추측 로직 삭제 — 미지정 시 호출자가 주는 운영 기본 계정 폴백(`connection_flow.py`, `meta_callback`). 테스트 갱신.

### 2. Meta 연결 admin 대리 허용

- `/meta/connect`가 멤버십 직접 해석이라 admin이 409로 차단되던 것을 다른 write와 동일한 `_require_org_id_write`(X-Org-Id impersonation + 감사 기록)로 통일. 테스트 2건 갱신·추가.

### 3. 예산관리 목데이터 — 중소기업 벤치마크 재설정

- 기본 월 목표 1,000만 → **300만원**(management.py `_BUDGET`·wiring.py). 가정 규모: 광고비=매출의 5~15% 역산 → 월 매출 2천만~6천만 이커머스·리테일 소기업(5~20인). 근거: SNS헬프 2026·Ballast·아이보스.
- 데모 캠페인 일예산 합 50만 → **10만/일**(policy.DAILY_BUDGET_KRW 정합). mock 소진을 월중 누적으로 환산(live this_month와 동일 의미).
- 예산 페이지 상단에 "기본 목표 기준" 스트립(업종·규모/월 매출/광고비 비중/권장 월 광고비) 노출.
- `test_budget_router` 인메모리 한도 누수(다른 테스트로 새는 순서 의존) 원복 픽스처로 수정.

### 4. 이상 감지 — 탐지 기준·출처·판정 규칙 화면 노출

- `/run` 응답에 `assumptions`(일예산·CPM 앵커·정상범위·기준 CTR·판정 임계·출처) 추가 — 단일원천 `contracts/policy.py`·`exposure_model.py`.
- 판정 임계 상수화: `DEFICIT_THRESHOLD=0.5`·`MIN_CONSECUTIVE_HOURS=2` — 화면 표기("기대 노출의 50% 미만 2시간 연속")와 로직이 같은 값.
- 출처 링크(lebesgue.io·AdAmigo.ai·내부 문서 §4.4) 및 고장 주입 강도(CPM×1.5·낙찰률 25% 등) 화면 명시. mock 수치 ↔ `meta-data-sources.md` §4.4~4.6 정합 검산 완료.

### 5. UX 정비 (캠페인·예산)

- 지표 오해 방지 툴팁: 도달(고유 인원·일자 합≠총합), CVR(분모=링크 클릭), CPM(1,000회당 단가·합산 아님) — 목록·상세·일자별 헤더.
- 대표 광고 시안 "지면별 미리보기" 캡션, '삭제됨 포함' 범위 안내, 일예산 미상 사유 툴팁, 예산 미상 캠페인 차트의 '일예산 ₩0' 기준선 숨김.
- 계정 지갑 스트립 리디자인(스탯 블록+소진율 게이지) — 이후 반원 게이지(ArcGauge)로 발전.

### 6. 캠페인 이름 AI 자동 제안 (신규)

- `domain/management/naming.py` — 소재(ads: 제목·카피·카테고리·타깃필터)+시뮬 클릭의향으로 후보 3개. gpt-4o-mini 보강, 키 없음/실패 시 결정론 규칙 폴백(`[제품]_[타깃]_[목표]_[월]`).
- `GET /campaign-proposals/name-suggestions` + 집행 모달 "AI 추천" 칩 3개(클릭 시 입력 채움). 라이브 검증: 실제 소재("댕댕이…")로 생성·채움 확인.

### 7. 자동화 6종 (신규)

| 기능 | 내용 | 원칙 |
|---|---|---|
| UTM 자동 부착 | 집행 URL에 `utm_source=clickme&utm_campaign={키}` (기존 utm 존중) | 외부 분석 식별 |
| 이상 스캔 자동화 | 이상 감지 진입 시+10분 주기(탭 숨김 스킵) | rate limit 배려 |
| 빈도 피로 감지 | 최근 7일 frequency≥3(`FATIGUE_FREQUENCY`, §4.6) → AUDIENCE_FATIGUE + 제너레이터 CTA | 실측 배선 |
| 예산 리밸런싱 제안 | 최근 7일 CPC 격차 1.2배+ 시 저효율→고효율 20% 이동 제안, 적용은 budget-commit 2건 | 자동 집행 없음(HITL) |
| 주간 리포트 | 최근 7일 실측 총합·캠페인별·하이라이트·다음 액션(결정론) — 모니터링 모달 | LLM 미사용 |
| 월 목표 계산기 | 월 매출 입력 → 권장(5~15%)·표준 10% 적용 | 벤치마크 승격 |

## 같은 브랜치의 다른 터미널 작업 (검증 범위에 포함, 상세는 해당 작업자 세션 참조)

- 홈 대시보드(`/manage`)·사이드바 '홈' 메뉴, `StatCard`(스파크라인·델타)·`ArcGauge`·`SpendDonut`·`CampaignRadar`(캠페인 성격 비교)·`ConversionFunnel`·`trend.ts`·`metaObjective.ts`.
- 캠페인 테이블 헤더 정렬 + 핵심/전체 지표 토글, 모니터링 조회 기간(전체/30일/이번달) 토글, compare·simulation 페이지 수정, 지갑·지표 설명 접이식(ⓘ) 등.
- 위 변경 전부 이번 전수 검증(빌드·린트·pytest·실측 대조)에 포함되어 통과.

## 남은 이슈 / 메모

- `_DEMO_PINNED_CAMPAIGN_IDS`(reader.py) — 발표용 핀. **발표 후 비우기**(주석에 명시됨).
- `ExecuteFromSimulation` 집행 게이트 임시 해제(EXEC_CIR 0/EXEC_REJ 1.0, "TEST" 주석) — 운영 복원 시 0.2/0.2.
- ProjectPanel useEffect deps ESLint 경고 1건(타 작업 영역, 에러 아님).
- 알림 벨은 채팅 알림 시스템 소유라 이상 스캔 벨 통합은 채팅 담당과 조율 필요.
- `/일일정리` 슬래시 명령어는 프로젝트·유저 어디에도 정의돼 있지 않아 이 문서(일일정리 겸용)로 대체함.
- 레포 루트의 `전체-점검-리포트-2026-06-28.md`(다른 세션 산출물)는 커밋에서 제외 — 위치·포함 여부 결정 필요.
