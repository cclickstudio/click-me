# 메모리 일원화 작업 체크리스트 (2026-07-03)

> 멘토링 반영 — 롱텀 메모리 = 기능 수행 히스토리(chat_execution_history) 단일화.
> 원칙: 쓰기는 코드(결정적), LLM은 파생·읽기 시점만. 에이전틱 RAG(KB 파이프라인)는 전 작업 무접촉.

## 1차 웨이브 — 메모리 일원화 (채팅 레이어, 단독 가능)

- [x] **작업 0. 테이블 개명** — `chat_long_term_memory→chat_session_summaries`, `execution_history→chat_execution_history` (0006 + models + history.py)
- [ ] **작업 1. sim/gen 이중 적재 정리** — save_template·infer_profile 조회처를 chat_execution_history로 교체 → spawn_persist의 LTM 적재·user_profile_inferred 증적 제거
- [ ] **작업 2. 표준 메모리 잔재 제거** — _spawn_memory_capture·_spawn_remember·remember/recall 도구·memory_store.py 삭제, _recall_memory_context 소스를 히스토리+브랜드프로파일로 교체(인터페이스 유지), prompts.py 라우팅 통합
- [ ] **작업 3. management_user_memory drop (0007)** — models 클래스 삭제 + 0001_baseline의 ix_user_memory_scope 라인 제거(신규 DB 가드)

## 2차 웨이브 — 롱텀 메모리 기능 완성

- [ ] **작업 4. record_execution core 이동 + management 액션 적재** — actor:"user", 기밀 화이트리스트, CLAUDE.md 문서화
- [ ] **작업 5. recall_history 확장** — 날짜 범위·feature_type 파라미터
- [ ] **작업 6. 자동 행동 워커** — APScheduler + 룰 카탈로그 11개(A탐지5·B자동실행2·C제안2·D분석2), Tier 게이트(AUTO_APPROVE_MAX_TIER), executor 다이렉트 호출, actor:"auto" 기록

## 팔로업·주의

- [ ] 0005(팀원) 머지 후 0006 down_revision 갱신 — 잊으면 multiple heads
- core/models.py 변경 = 공통부 → 팀 사전 공지
- 커밋 = 작업당 1개, 백엔드 커밋 전 Ruff. 푸시는 별도 지시 시(3k→management 순)
- 하지 않는 것: role 체계 / 매니지먼트 자체 대화 테이블 / 시뮬·생성 팀 코드
