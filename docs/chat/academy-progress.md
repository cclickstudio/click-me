- 2026-06-25 09:09 R4-1 ✅ — `ClioKbChunk` 모델과 Alembic 025 `clio_kb_chunks` 테이블 추가. 개인 DB(ep-soft-band)에서 024→025 up, 컬럼 확인, 025→024 down, 024→025 복구 검증 완료. `ruff check . --fix`, `import api.main` 통과.
- 2026-06-25 09:18 R1 ✅ — 시뮬 용어+정의 사전 `simulation_glossary.md` 63개 항목 추가. `domain.simulation.assistant.kb_ingest`로 개인 DB(ep-soft-band) 적재 완료, `simulation_kb_chunks` 69청크 확인, `SimKbRetriever` 검색 스모크와 `import api.main` 통과.
- 2026-06-25 09:21 R2 ✅ — 제너 용어+정의 사전 `creative_glossary.md` 72개 항목 추가. `domain.generator.assistant.kb_ingest`로 개인 DB(ep-soft-band) 적재 완료, `generator_kb_chunks` 79청크 확인, `GenKbRetriever` 검색 스모크와 `import api.main` 통과.
- 2026-06-25 09:29 R3 ✅ — 매니지 용어+정의 사전 `management_glossary.md` 71개 항목 추가 및 `_SOURCE_META` 등록. 개인 DB(ep-soft-band)에 기존 019 매니지 지식 스키마가 미적용이라 저장소 migration `019_management_knowledge_and_agent_state.py`의 `upgrade()`를 Alembic Operations로 적용 후 인제스트 완료, `management_kb_chunks` 88청크와 `document_id` 88건 연결 확인, `KbRetriever` 검색 스모크와 `ruff check . --fix`, `import api.main` 통과.
- 2026-06-25 09:34 R4-2 ✅ — CLIO 전용 광고 일반지식 KB `advertising_general_knowledge.md` 70개 섹션과 `domain.chat.kb_ingest` 추가. 개인 DB(ep-soft-band)에 `clio_kb_chunks` 70청크 적재 확인, chunk parser 스모크와 `ruff check . --fix`, `import api.main; import domain.chat.kb_ingest` 통과.
- 2026-06-25 09:39 R4-3 ✅ — `ClioKbRetriever` 추가 및 `advise_node`에 CLIO KB 컨텍스트 주입·`meta.citations` 연결. 광고 일반 질문은 `clio_kb_chunks` top-k 근거를 사용하고 비광고 한도 경로는 기존대로 유지. retriever 검색 스모크, 오케스트레이터 CLIO 응답 citations 스모크, `ruff check . --fix`, `import api.main; import domain.chat.orchestrator; import domain.chat.retriever` 통과.
- 2026-06-25 09:43 L1 ✅ — 기존 병합 이력의 `core.tracing.make_trace_config` 구현을 가이드 §4 기준으로 재검증. `tests/test_tracing.py` 5건, `ruff check core/tracing.py tests/test_tracing.py`, standalone import/config 스모크 통과.
- 2026-06-25 09:50 L2-1 ✅ — 채팅 LangGraph `_State`를 `MessagesState` 상속으로 전환하고, 매니지먼트 `build_checkpointer()` 재사용 체크포인터를 `graph.compile(checkpointer=...)`에 연결. `graph.ainvoke`에 임시 `thread_id` configurable과 `messages` seed를 전달해 L2-2 전 API 계약을 유지. `ruff format .`, `ruff check . --fix`, `import api.main`, graph compile 스모크 통과.
- 2026-06-25 09:50 L2-2 ✅ — `/api/chat/complete`와 `/api/chat/approve`에서 체크포인터 `thread_id`를 채팅 `session_id`로 고정하고, `ChatRequest`/`ApproveRequest`에 구 `thread_id` 호환 입력을 추가. 오케스트레이터 `ChatTurn.thread_id`와 응답 meta의 `thread_id`/`session_id`를 연결. Windows 로컬 `InMemorySaver` 폴백, thread 매핑 스모크, `ruff format .`, `ruff check . --fix`, `import api.main` 통과.
