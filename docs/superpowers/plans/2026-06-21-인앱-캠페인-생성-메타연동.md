# 인앱 캠페인 생성 → Meta 실연동 (매니지먼트 전용)

작성 2026-06-21 · management(4-2) · 범위: `domain/management/*` + `api/routers/management.py` + `tests/management/`. 다른 도메인·프론트 미수정.

## 컨텍스트 노트

- 흐름 — `/campaigns/create-proposal` → 제안(Tier3) → `/approve` → `/execute` → `writer.create_campaign`.
- 모드 — `settings.management_execution_mode`(dry_run | validate_only | live). `use_mock=False` + `meta_access_token`일 때만 실전송(writer `_SENDING_MODES`).
- 안전판 — 생성은 항상 PAUSED, Tier3 건별 승인, 모드 게이트. 1~5 내내 유지.
- 자격(.env) — `META_ACCESS_TOKEN`(ads_management 보유 확인됨), `META_AD_ACCOUNT_ID`, `META_PAGE_ID`(리드폼 필수), `META_GRAPH_API_VERSION`.
- **LIVE 실생성(2)·실집행(5)은 사용자가 실 토큰으로 실행.** 여기 테스트는 httpx MockTransport 계약 테스트(요청 스키마·매핑 검증, 네트워크 0).

## 체크리스트

- [x] **Task1** 이름·목표 전달 + VALIDATE_ONLY 준비 — `CampaignConfig.name`·objective(leads) 확장, `create-proposal`(실계정), `writer.create_campaign`(objective 매핑), 계약 테스트 2 ✅
- [x] **Task2** LIVE 게이트 해금(코드) — `_resolved_execution_mode`(use_mock→MOCK 봉인, 실모드만 설정 따름), `/approve`·`_get_executor` 배선, 게이팅 테스트 5 ✅ · **실 토큰 생성 검증은 사용자**
- [x] **Task3** `writer.create_adset` — 예산·타겟·LEAD_GENERATION/LINK_CLICKS·promoted_object(page_id)·ON_AD, 중첩객체 json.dumps, 계약 테스트 3 ✅
- [x] **Task4** `writer.create_lead_form`(페이지 노드·개인정보 URL) + `create_ad`(폼 CTA 크리에이티브) + 계약 테스트 ✅
- [x] **오케스트레이션** `create_full_campaign` — 캠페인→광고세트→폼→광고 id 스레딩, executor CREATE_CAMPAIGN이 호출, Protocol·FakeWriter 배선, 체인 테스트 ✅ · **버튼→전체 구조 생성**
- [x] **Task5** `writer.activate`(status=ACTIVE) + 테스트 ✅ · **실 활성화(과금 시작)는 Ads Manager에서 사람이** (자동 호출 안 함)
