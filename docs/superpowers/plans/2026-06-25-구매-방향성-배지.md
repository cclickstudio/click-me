# 구매 축 방향성 배지 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 성과 비교 카드의 구매 축을 클릭 축과 대칭으로 만들어, 구매의도(예측, /5)와 CVR(실측, %)을 각자 기준선 통과(강함/약함) 배지로 나란히 보여준다.

**Architecture:** management 도메인의 `compute_before_after`에 파생 boolean 필드 2개(`purchase_pred_strong`, `purchase_act_strong`)를 추가하고(영속 컬럼 아님 → DB 마이그레이션 없음), 프론트 `compare` 카드에 클릭 방향성 블록을 복제한 "구매 방향성" 블록을 추가한다. **직접 환산 금지·verdict 불변** 원칙을 유지하며 배지는 표시 전용이다.

**Tech Stack:** Backend FastAPI + Pydantic(frozen Contract) + pytest(uv) · Frontend Next.js(TS) + Tailwind(pnpm).

---

## 사전 상태 (중요)

- `backend/tests/management/test_before_after_service.py`는 **이미 수정된 상태(git status `M`, 커밋 안 됨)** 다. 구매 축 테스트 5개(아래)가 이미 작성돼 있고 현재 **실패(red)** 한다. 새 테스트를 쓰지 않고 이 테스트들을 green으로 만든다.
  - `test_purchase_direction_strong_when_purchase_intent_and_cvr_cross_baselines`
  - `test_purchase_prediction_weak_below_purchase_intent_baseline`
  - `test_purchase_actual_unknown_when_cvr_is_null`
  - `test_purchase_prediction_unknown_when_prediction_is_missing`
  - `test_purchase_direction_does_not_change_click_axis_verdict`
- 커밋 시 이 테스트 파일을 **함께 포함**한다.

## File Structure

- `backend/domain/management/comparison/schemas.py` — `BeforeAfter` Contract에 파생 필드 2개 추가. (책임: 비교 산출물 DTO 정의)
- `backend/domain/management/comparison/service/before_after_service.py` — `CVR_STRONG` 상수 + 구매 축 계산 로직. (책임: 전/후 방향성 판정 유스케이스)
- `backend/tests/management/test_before_after_service.py` — 이미 red 테스트 존재(수정 불필요, 커밋만).
- `frontend/src/lib/api.ts` — `BeforeAfterItem` 타입에 필드 2개 추가. (책임: API 응답 타입)
- `frontend/src/app/(app)/manage/compare/page.tsx` — "구매 방향성" 표시 블록 추가. (책임: 성과 비교 화면)

---

## Task 1: 백엔드 — 구매 축 파생 필드 추가

**Files:**
- Modify: `backend/domain/management/comparison/schemas.py` (BeforeAfter 클래스, 현 120~121줄 아래)
- Modify: `backend/domain/management/comparison/service/before_after_service.py`
- Test: `backend/tests/management/test_before_after_service.py` (이미 존재, red)

- [ ] **Step 1: 기존(이미 작성된) 구매 축 테스트가 실패하는지 확인 (red)**

Run: `cd backend && uv run pytest tests/management/test_before_after_service.py -k purchase -v`
Expected: FAIL — `AttributeError: 'BeforeAfter' object has no attribute 'purchase_pred_strong'` (또는 동등한 속성 없음 에러)

- [ ] **Step 2: `BeforeAfter` 스키마에 필드 2개 추가**

`backend/domain/management/comparison/schemas.py`의 `BeforeAfter` 클래스에서, 기존 마지막 두 필드

```python
    pred_strong: bool | None = None  # 클릭 의향률 강함(≥0.2) 통과 — 예측 없으면 None
    act_strong: bool | None = None  # 실측 CTR 양호(≥1%) 통과 — 판정 불가(노출0·미연결)면 None
```

바로 아래에 다음을 추가한다:

```python
    purchase_pred_strong: bool | None = None  # 구매의도 강함(≥3.5/5) 통과 — 예측 없으면 None
    purchase_act_strong: bool | None = None  # 실측 CVR 양호(≥2%) 통과 — CVR null(추적 전)이면 None
```

- [ ] **Step 3: 서비스에 `CVR_STRONG` 상수 + 구매 축 실측 헬퍼 추가**

`before_after_service.py`에서 기존 상수 `CTR_STRONG = 0.01` 바로 아래에 추가한다:

```python
#: 구매 축 실측 "양호" 기준선 — 이 화면의 운영 기준일 뿐 "절대적으로 좋은 CVR"이 아니다
#: (제품·업종·퍼널에 따라 달라짐). 클릭 축 CTR_STRONG(1%)과 같은 자리, 각 축 독립(척도 환산 아님).
CVR_STRONG = 0.02
```

그리고 기존 `_actual_strong` 함수 바로 아래에 구매 축 실측 헬퍼를 추가한다(CVR null → None):

```python
def _purchase_actual_strong(actual: RealOutcome) -> bool | None:
    """구매 축 실측 양호 = CVR 기준 단독. CVR 미추적(None)이면 판정 불가(None)."""
    if actual.cvr is None:
        return None
    return actual.cvr >= CVR_STRONG
```

- [ ] **Step 4: `compute_before_after`에서 구매 축 필드 계산 + 반환**

`compute_before_after` 함수를 아래로 교체한다(클릭 축 판정·rationale·interpretation 로직은 그대로, 구매 축 두 변수만 추가). 핵심: 구매 필드는 **예측이 있을 때만** 계산하고(예측 None이면 둘 다 None 유지), `purchase_act_strong`은 CVR 유무로 결정한다.

```python
def compute_before_after(
    campaign_id: str,
    name: str,
    prediction: PredictionSnapshot | None,
    actual: RealOutcome,
) -> BeforeAfter:
    """전(예측)·후(실측) 묶음 + 클릭 축 방향성 판정 + 구매 축 방향성 배지 + 보조 해석."""
    interpretation = ""
    pred_strong: bool | None = None
    act_strong: bool | None = None
    purchase_pred_strong: bool | None = None
    purchase_act_strong: bool | None = None
    if prediction is None:
        verdict, rationale = BeforeAfterVerdict.UNKNOWN, "시뮬 미연결 — 예측 데이터 없음"
    elif actual.impressions == 0:
        verdict, rationale = BeforeAfterVerdict.UNKNOWN, "집행 데이터 부족 — 노출 0"
        pred_strong = prediction.click_intent_rate >= CLICK_INTENT_STRONG
        purchase_pred_strong = prediction.purchase_intent >= PURCHASE_HIGH
        purchase_act_strong = _purchase_actual_strong(actual)
    else:
        pred_strong = prediction.click_intent_rate >= CLICK_INTENT_STRONG
        act_strong = _actual_strong(actual)
        purchase_pred_strong = prediction.purchase_intent >= PURCHASE_HIGH
        purchase_act_strong = _purchase_actual_strong(actual)
        if pred_strong and act_strong:
            verdict, rationale = BeforeAfterVerdict.ALIGNED, "예측대로 좋음 — 실측이 뒷받침"
        elif pred_strong and not act_strong:
            verdict, rationale = (
                BeforeAfterVerdict.UNDERPERFORMED,
                "예측은 높았으나 실측은 약함 — 소재·타겟 점검",
            )
        elif not pred_strong and act_strong:
            verdict, rationale = (
                BeforeAfterVerdict.OVERPERFORMED,
                "예측보다 실측이 좋음 — 증액 검토",
            )
        else:
            verdict, rationale = BeforeAfterVerdict.ALIGNED, "예측대로 낮음 — 관망"
        interpretation = _interpretation(prediction, act_strong)
    return BeforeAfter(
        campaign_id=campaign_id,
        name=name,
        prediction=prediction,
        actual=actual,
        verdict=verdict,
        rationale=rationale,
        interpretation=interpretation,
        pred_strong=pred_strong,
        act_strong=act_strong,
        purchase_pred_strong=purchase_pred_strong,
        purchase_act_strong=purchase_act_strong,
    )
```

> **설계 노트 (의도적 선택).** `prediction is None`이면 `purchase_act_strong`은 CVR이 있어도 None이다. 이는 "실측 CVR이 2%를 넘었는가"를 못 봐서가 아니라, **방향성 배지는 예측↔실측 비교쌍을 전제**하기 때문이다. 기존 클릭 축 `act_strong`도 동일하게 prediction None이면 None(CTR 값 유무와 무관)이라, 두 축의 규칙을 일치시킨 것이다. 실측만 독립 판정하려면 클릭 축까지 같이 바꿔야 하며 이번 범위 밖이다.

- [ ] **Step 5: 전체 테스트 green 확인 (회귀 포함)**

Run: `cd backend && uv run pytest tests/management/test_before_after_service.py -v`
Expected: PASS — 구매 축 5개 + 기존 클릭/UNKNOWN/보조해석 케이스 전부 통과(기존 verdict 케이스 불변)

- [ ] **Step 6: Ruff 정렬 (CLAUDE.md 워크플로 ①)**

Run: `cd backend && uv run ruff format . && uv run ruff check . --fix`
Expected: 변경 없음 또는 자동 정렬만, 에러 0

- [ ] **Step 7: 커밋 (수정된 테스트 파일 포함)**

```bash
git add backend/domain/management/comparison/schemas.py \
        backend/domain/management/comparison/service/before_after_service.py \
        backend/tests/management/test_before_after_service.py
git commit -m "add: 성과 비교 구매 축 방향성 배지 파생 필드

- BeforeAfter에 purchase_pred_strong(구매의도≥3.5)·purchase_act_strong(CVR≥2%) 추가
- CVR null(추적 전)·예측 미연결이면 None, verdict는 클릭 축 단독 유지(불변)
- 직접 환산 아님 — 각 축 독립 기준선 통과만 판정

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: 프론트 — 구매 방향성 블록 추가

**Files:**
- Modify: `frontend/src/lib/api.ts` (BeforeAfterItem 인터페이스, 현 116~117줄 아래)
- Modify: `frontend/src/app/(app)/manage/compare/page.tsx` (클릭 방향성 블록 아래)

- [ ] **Step 1: `api.ts`의 `BeforeAfterItem`에 필드 2개 추가**

`frontend/src/lib/api.ts`의 `BeforeAfterItem`에서 기존

```typescript
  pred_strong?: boolean | null; // 클릭 의향률 강함(≥20%) 통과 — 예측 없으면 null
  act_strong?: boolean | null; // 실측 CTR 양호(≥1%) 통과 — 판정 불가면 null
```

아래에 추가한다:

```typescript
  purchase_pred_strong?: boolean | null; // 구매의도 강함(≥3.5/5) 통과 — 예측 없으면 null
  purchase_act_strong?: boolean | null; // 실측 CVR 양호(≥2%) 통과 — CVR null(추적 전)이면 null
```

- [ ] **Step 2: 카드에 "구매 방향성" 블록 추가**

`compare/page.tsx`의 `BeforeAfterCard` 안에서, **클릭 방향성 블록(`{/* 핵심 방향성 — 클릭 의향률 ... */}` 로 시작하는 `<div ... mb-3>` 닫힘 `</div>`) 바로 다음, 보조 grid(`{/* 보조 — 예측 KPI ... */}`) 바로 앞**에 다음 블록을 삽입한다. 기존 `StrongBadge` 컴포넌트를 그대로 재사용한다.

```tsx
      {/* 구매 방향성 — 구매의도(예측, /5) ⟷ CVR(실측, %). 척도가 달라 직접 환산 아님 — 각자 기준선 통과로만 비교. */}
      <div className="rounded-xl border border-[#E5E8EB] dark:border-[#2D3748] bg-[#F9FAFB] dark:bg-[#252D3D] p-4 mb-3">
        <p className="text-[11px] text-[#8B95A1] mb-2.5">
          구매의도와 CVR은 서로 환산하지 않고, 각자 기준선 통과 여부만 봅니다.
        </p>
        <div className="flex items-center gap-3">
          <div className="flex-1">
            <p className="text-[11px] text-[#8B95A1]">구매의도 · 예측</p>
            <p className="text-lg font-extrabold text-[#191F28] dark:text-[#F2F4F6] tabular-nums">
              {p ? `${p.purchase_intent.toFixed(1)}/5` : '—'}
            </p>
            <StrongBadge strong={item.purchase_pred_strong} strongLabel="강함 ≥3.5" weakLabel="약함 <3.5" />
          </div>
          <span className="text-xl text-[#8B95A1] shrink-0">⟷</span>
          <div className="flex-1 text-right">
            <p className="text-[11px] text-[#8B95A1]">
              CVR · 실측 <span className="text-[#3182F6]">(실 Meta)</span>
            </p>
            <p className="text-lg font-extrabold text-[#191F28] dark:text-[#F2F4F6] tabular-nums">
              {a.cvr != null ? `${(a.cvr * 100).toFixed(1)}%` : '추적 전'}
            </p>
            <StrongBadge strong={item.purchase_act_strong} strongLabel="양호 ≥2%" weakLabel="약함 <2%" />
          </div>
        </div>
      </div>
```

(참고: `p`는 `item.prediction`, `a`는 `item.actual`로 이미 `BeforeAfterCard` 상단에 선언돼 있어 그대로 쓴다. `StrongBadge`는 이미 `strongLabel`/`weakLabel` props를 받으므로 예측="강함/약함", 실측="양호/약함"을 그대로 넘기면 되고, `strong == null`이면 "판정 불가"를 자동 표시한다 — 컴포넌트 수정 불필요.)

- [ ] **Step 3: 린트 통과 확인**

Run: `cd frontend && pnpm lint`
Expected: 에러 0 (CI `ci.yml`의 frontend ESLint와 동일)

- [ ] **Step 4: 빌드 확인**

Run: `cd frontend && pnpm build`
Expected: 빌드 성공(타입 에러 0)

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/lib/api.ts "frontend/src/app/(app)/manage/compare/page.tsx"
git commit -m "add: 성과 비교 카드에 구매 방향성 배지 블록

- 구매의도(예측,/5) ⟷ CVR(실측,%) 기준선 통과 배지(클릭 축과 대칭)
- 직접 환산 아님 캡션 명시, CVR 미추적이면 '추적 전'·'판정 불가'

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## 완료 기준

- `uv run pytest tests/management/test_before_after_service.py -v` 전부 PASS, 기존 클릭 축 verdict 케이스 불변.
- `pnpm lint` · `pnpm build` 통과.
- 성과 비교 카드에 클릭/구매 두 방향성 블록이 대칭으로 표시되고, CVR 미추적 캠페인은 "추적 전 / 판정 불가"로 표기.
- 문서·UI 모두 "구매의도와 CVR 직접 환산 아님" 명시.
