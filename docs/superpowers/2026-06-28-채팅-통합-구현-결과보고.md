# 채팅 통합 — 구현 결과 보고서 (2026-06-28)

> 계획: `plans/2026-06-27-채팅-통합-계획.md` · 설계: `specs/2026-06-27-chat-kb-rag-구조도.md`
> 브랜치: `feat/management-3k` = `feat/management` = `aed9512` (양쪽 동일)
> 작성: 경구 · 자율 루프 실행 + 실측 검증

---

## 1. 한눈에 — 무엇을 했나

집행 전후 광고 채팅(4-4)을 **경구 Deep Agent 베이스 + 보은 카드/RAG + 도연 일반KB/시맨틱LTM + 태호 체크포인터** 강점으로 통합했다. 설계서의 갭 11개 + Alembic 029를 자율로 구현·검증하고, 추측이 아니라 **실측 데이터가 설계 구멍을 드러내게** 했다.

| 지표 | 결과 |
|---|---|
| 구현 갭 | G1~G7 + M1/M2/M3/M4/M6 + Alembic 029 = **12개 완료** |
| 백엔드 테스트 | **501 passed, 0 failed** (3회 반복) |
| 환각(Faithfulness) | **0.920 → 1.000** (judge×3, 3단계 부분점수) |
| 검색 품질 | Hit@5 0.95 / MRR 0.84 / Context Precision 0.84 (전 목표 충족) |
| 리눅스 PG 체크포인터 | docker round-trip **실증** (Windows 한계 돌파) |
| 시맨틱 LTM | "목표 뭐였지?" → 의미적 회수 검증 |
| 프론트 카드 | RESULT/REVIEW/ACTIONBAR + trust boundary **브라우저 시각 검증** |
| KB | 15문서·64청크 → **19문서·236청크** (일반지식 2 + Meta레퍼런스 + Awareness) |

---

## 2. 설계 대비 구현 충실도 (실측 대조)

> "진짜 설계한 대로 구현됐나" — spec 갭 정의 ↔ 실제 코드 `file:line` 대조 결과.

### ✅ 설계대로 구현된 것 (12/12)

| 갭 | 설계 의도 | 구현 위치 | 검증 |
|---|---|---|---|
| G1 | kb_ingest 서브디렉토리 적재 + 시뮬 오염 차단 | `kb_ingest.py` rglob+`_SUBDIR_ALLOWLIST` | DB 적재 로그·시뮬 0청크 |
| G2 | Deep Agent가 suggested_action 캐리(카드 버그) | `deep_agent.py:_state_to_result` | 합성 state 캐리 |
| G3 | SubagentResult→AskResult 어댑터 | `chat.py:_sub_to_ask` | compose_turn→4카드 |
| G4 | ADVISE KB 게이트(cosine max + 인용검증) | `wiring.py:_try_kb_advise` | "ROAS"→KB cos 0.674 / "날씨"→CLIO |
| G5 | source_type 스코핑(일반↔특화 분리) | `retriever.py` `source_types` | 오염 0.735→0.835 회복 |
| G6 | 카드 직렬화 + 프론트 렌더러 | `chat.py:meta.cards` + `page.tsx` | **브라우저 시각 검증** |
| G7 | 체크포인터 단일화 + 견고 config | `wiring.py`+`checkpointer.py`, 중복 삭제 | docker 리눅스 round-trip |
| M1 | recall 배선 복구(write-only→회수) | `memory_context` 4파일 관통 | 테스트 통과 |
| M2 | LLM 추출 게이트 + dedup upsert | `chat.py:_extract_memory`+`aput` upsert | 선호 저장/잡담 skip |
| M3 | anon 격리(cross-user 누출 차단) | `chat.py` user_id 게이트 | 로그인만 LTM |
| M4 | 단기 히스토리 중복 차단 | `deep_agent.py` 요청별 thread_id | orchestrator 3회 |
| M6 | 세션 요약 + 시맨틱 회수(도연) | `_summarize_session`+`embedding`+코사인 | "목표 뭐였지?"→의미 회수 |

### ⚠️ 설계에서 의도적으로 벗어난 것 (이탈 사유 명시)

| 항목 | 설계 | 구현 | 이유 |
|---|---|---|---|
| **G6 SSE 방식** | 풀 TurnEnvelope(conclusion_delta/card_ready/final) | **중간경로**(meta.cards 직렬화, 기존 token/done 유지) | 프론트가 이미 meta 렌더 보유 → 풀 프로토콜 재작성은 회귀 위험↑. 카드 화면 목표를 최소 변경으로 달성. composer는 그대로 사용(trust boundary 보존) |
| **G4 임계값** | 계획 규칙 "C2 폴백률 최저" | **C1 전통과 + C2 폴백 최대** 로 정정 | 원 규칙이 로직 반전(C2는 폴백이 정답). 스윕 데이터로 0.35 확정 |
| **M5(체크포인터 이원화)** | 별도 갭 | **G7에 흡수** | 체크포인터 단일화가 M5를 동시 해소 |

### ❌ 미구현(의도적 후순위 — 설계가 명시한 비목표/후속)

| 항목 | 상태 |
|---|---|
| Deep Agents 풀 기계장치(계획도구·가상FS) | **챗 범위 비목표**(spec §10.7) — Supervisor 패턴이 정답 |
| 풀 TurnEnvelope SSE | 중간경로로 대체(위) |
| dev 병합 | PR 리뷰 후(사용자 트리거) |
| 운영 EC2 검증 | docker 리눅스로 동등 검증 |

---

## 3. 핵심 결과 — 데이터 기반 발견 5건

설계가 단단해진 건 추측이 아니라 **실측이 구멍을 드러냈기 때문**이다.

### ① 환각 측정 인프라 고장 — 가짜 1.0
- Faithfulness judge `gemini-2.0-flash-lite`가 **단종(404)** → 모든 judge 호출 실패 → 에러를 faithful로 오집계 → **"1.00"은 가짜**.
- 수정: `gemini-2.5-flash-lite` 교체 + 에러를 not_faithful 보수처리(`n_error` 노출).

### ② 일반 KB가 management 검색을 오염
- 도연 일반KB 162청크 적재 시 management 특화 검색 **Context Precision 0.835 → 0.735(목표 미달)**.
- 수정: `source_type` 스코핑(ADVISE=일반, MANAGE=특화) → **0.835 완전 회복.** 보은 §10이 "선택"이 아니라 필수였음을 입증.

### ③ 환각 개선 — 게이트의 진짜 가치는 C5·C4
| 유형 | 개선전(CLIO) | 개선후(게이트) |
|---|---|---|
| C1 KB-답변가능 | 1.000 | 1.000 |
| C2 KB-밖 | 1.000 | 1.000 |
| C3 거짓전제 | 1.000 | 1.000 |
| **C4 수치함정** | 0.875 | **1.000** |
| **C5 도메인-밖** | 0.556 | **1.000** |
| **전체** | **0.920** | **1.000** |
- gpt-4o-mini는 이미 일반개념·거부·전제교정(C1~C3)을 잘함 → 게이트 가치는 **도메인-밖(C5)·수치 날조(C4)**. **grounding 프롬프트**가 fabrication을 막은 것.

### ④ Windows 한계 돌파 — docker 리눅스로 PG 실증
- Windows는 `sys.platform=win32`라 PG 체크포인터 스킵(검증 불가).
- **docker python:3.11-slim(리눅스)에서 Neon 실연결 → setup·체크포인트 round-trip(저장→복구) 성공.** HITL/재시작 복구가 진짜 동작함을 확인.

### ⑤ 시맨틱 LTM 회수
- recency가 아니라 의미적으로 회수: "내 광고 목표가 뭐였지?" → (최신=플랫폼이 아닌) **"트래픽 목표"를 회수.**

---

## 4. 검증 방식 (재현 가능)

- **검색·환각:** `uv run python -m domain.management.assistant.rag_eval --k 5` / `evals/hallucination_eval.py`(C1~C5 골든셋, judge×3).
- **테스트:** `uv run python -m pytest tests/management tests/assistant tests/orchestration` → 501 passed.
- **리눅스 PG:** docker python:3.11-slim + Neon round-trip.
- **프론트:** `pnpm tsc/build/eslint` + 브라우저 카드 렌더 스크린샷.
- **환경 메모:** Windows 콘솔은 `PYTHONUTF8=1`, `KbRetriever`는 `set -a && . ./.env`로 OPENAI_API_KEY env 필요.

---

## 5. 남은 것

- **풀 TurnEnvelope SSE** — 중간경로로 카드는 동작. 풀 전환은 선택적 후속.
- **dev 병합** — PR 리뷰 후.
- **운영 EC2 실검증** — docker 동등 검증 완료, 실 배포 시 재확인.

> 상세 갭별 개선전/후·테스트 3회 기록은 `plans/2026-06-27-채팅-통합-계획.md` 「작업 로그」에, 설계·표준 정합성은 `specs/2026-06-27-chat-kb-rag-구조도.md`에 있다.
